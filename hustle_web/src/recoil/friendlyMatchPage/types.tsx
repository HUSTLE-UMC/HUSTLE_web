export interface menuTypes {
  id: number;
  label: string;
  isSelected: boolean;
}

export interface submitTypes {
  id: number;
  msg: string;
  isSubmitted: boolean;
}

export interface clubListsTypes {
  id: number;
  img: string;
  sport: string;
  title: string;
  location: string;
  date: string;
}
