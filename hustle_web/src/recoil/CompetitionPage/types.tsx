export interface menuTypes {
  id: number;
  label: string;
  isSelected: boolean;
}

export interface sortTypes {
  id: number;
  sort: string;
  label: string;
}

export interface CompetitionTypes {
  id: number;
  img: string;
  sort: string;
  title: string;
  location: string;
  date: string;
}
