import React from 'react';
import {HomeContainer} from "./styles";
import Header from '../../components/Header';

const Home = () => {

    return (
        <HomeContainer>
            <Header/>
        </HomeContainer>
    );
};

export default Home;
